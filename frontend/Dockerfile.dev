FROM node:20-alpine

WORKDIR /app

# Install dependencies — dung npm ci de reproducible, khong modify lock file
COPY package.json package-lock.json ./
RUN npm ci

# Copy source code
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
